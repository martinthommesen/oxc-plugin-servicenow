# Implementation plans

Generated by the improve skill on 2026-08-19 at commit `c987eee`.
Reconciled on 2026-08-20 at reviewed PR #51 head `b87972a`.

Plans 001–005 record completed work from the first audit.
Plans 006–016 address all confirmed findings from the full PR #51 review and the follow-up review supplied by the maintainer.
Execute plans 006–016 in dependency order.
Read each plan fully, honor its STOP conditions, and update its status row after execution.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Run the plugin under real oxlint and real ESLint in CI | P1 | M | — | DONE |
| 002  | Fix script-type classification, pragma detection, and per-file cost | P1 | M | 001 (soft) | DONE |
| 003  | Make the autofix/suggestion surface honest, safe, and tested | P1 | M | 001 (soft) | DONE |
| 004  | Fix five confirmed rule-level correctness bugs | P2 | M | 001 (soft) | DONE |
| 005  | Harden CI, packaging metadata, and the release path | P2 | M | 001 (soft) | DONE |
| 006  | Freeze PR #51 and create the real remediation stack | P1 | L | — | DONE |
| 007  | Rebuild the path-state evaluator with reachable JavaScript semantics | P1 | L | 006 | IN PROGRESS — implemented in #77; merge pending |
| 008  | Make bindings, scopes, closures, and analysis caches authoritative | P1 | L | 006, 007 | IN PROGRESS — implemented in #78; merge pending |
| 009  | Rebuild stateful rule lifecycles on the corrected evaluator | P1 | L | 006–008 | IN PROGRESS — implemented in #79; merge pending |
| 010  | Make the Fluent SDK registry authoritative and project-aware | P1 | L | 006, 008, 009 | IN PROGRESS — implemented in #80; merge pending |
| 011  | Make `Now.ID` semantics temporal and Fluent directives exact | P1 | M | 006, 008–010 | IN PROGRESS — implemented in #81; merge pending |
| 012  | Fix context profiles and bounded rule contracts | P1 | L | 006, 008–011 | IN PROGRESS — implemented in #82; merge pending |
| 013  | Narrow the public API and make user assets truthful | P1 | L | 007–012 | IN PROGRESS — implemented in #83; merge pending |
| 014  | Make tests, evidence, compatibility, and benchmarks honest | P1 | L | 007–013 | IN PROGRESS — implemented in #84; merge pending |
| 015  | Prove release governance and exact provenance | P1 | L | 006–014 | IN PROGRESS — implemented in #85; merge pending |
| 016  | Stage and release 2.0.0 from the reviewed stack | P1 | L | 006–015 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 first: it establishes real-host verification (oxlint + ESLint actually load and run the plugin). Plans 002–004 change rule behavior; without 001 their only safety net is the hand-rolled harness whose divergence from oxlint is itself a documented risk. All dependencies are soft — each plan can execute standalone.
- 001 and 005 both edit `package.json` scripts and `.github/workflows/ci.yml`; whichever runs second must reconcile, not revert.
- 002 and 004 both touch `src/rules/no-br-current-update.ts` (002 hoists classification into `before()`; 004 inverts the kind check to an allowlist). Order-independent, but the second executor must re-read the file.
- 003 and 004 both add tests near `tests/rules/glide-and-engine.test.ts`; merge conflicts are cosmetic.
- 002 removes a known false positive (`no-client-gliderecord` on display Business Rules) that 001's integration fixture must tolerate until 002 lands — 001's STOP conditions cover this.

## PR #51 dependency notes

- Execute plan 006 first. It archives `b87972a`, restores truthful status, and creates actual dependent pull requests.
- Execute plans 007 and 008 before any stateful-rule repair. They replace the unsound evaluator and binding foundation.
- Execute plan 009 after both foundation plans. Do not patch lifecycle consumers around the old interpreter.
- Plan 010 requires the corrected binding model and follows plan 009 in the stack. Plan 011 requires plans 008–010.
- Plan 012 consumes the completed evaluator, lifecycle, and Fluent semantic contracts.
- Plan 013 defines the intended 2.0 public and user-facing surface only after behavior is stable.
- Plan 014 makes acceptance, compatibility, and performance evidence authoritative for the completed implementation.
- Plan 015 is the only privileged release-automation pull request. It follows every implementation and evidence plan.
- Plan 016 is an operator-controlled release procedure. It does not authorize a merge, tag, publication, or issue closure by itself.

The stack is intentionally serial for review and rollback clarity. A maintainer can execute independent preparation work in parallel, but each pull request must retain its declared base.

## PR #51 finding coverage

Plans 006–016 cover the complete current review as follows:

- **Delivery, restack, and truthful status — plans 006, 014, and 016.** These plans cover the false completion ledger, stale PR-body evidence, the circular merge/release wording, the unsatisfied issue #75 restack, exact current-head proof, and final #58/#76 closure.
- **JavaScript evaluation and completion — plan 007.** This plan covers receiver and argument order, computed assignment targets, destructuring effects, `&&=`, `||=`, `??=`, duplicate uninitialized `var`, literal reachability, complex receivers, switch flow, labels, abrupt joins, loops, throw-point handlers, and `finally` replacement semantics. It also splits the divergent path-state closure into explicit program, evaluator, and object-store modules.
- **Bindings, scopes, closures, provenance, and caches — plan 008.** This plan covers duplicate binding coalescing, class-expression and static-block scope, parser/host parity, temporal escape, closure invocation or conservative summaries, pattern walking, source/project cache identity, immutable cached products, safe comment-token fallback, one constructor-provenance registry, and one structural pattern walker.
- **Stateful ServiceNow rules — plan 009.** This plan covers query recovery, query-modifier may-risk joins, GlideAggregate pending/committed epochs, GlideAjax missing/null/non-string values and multiple requests, `setNoCount` recovery and branch-local row-count use, scope/release-aware executors, cursor-loop identities, malformed bulk filters, callback callability, and GlideElement collection duplicates, nested values, and shadowed extractors. It also replaces duplicate cursor-loop walkers and deduplication.
- **Fluent authority and imports — plan 010.** This plan covers published SDK patches, reproducible declaration integrity, version ranges, current-default selection, Australia/Zurich policy, temporal named and namespace aliases, project barrel re-exports, and packed multi-file consumers.
- **`Now.ID` and directives — plan 011.** This plan covers aliases, wrappers, duplicate IDs, semantic parents, plain versus compound assignment sinks, nested scopes, exact previous-line attachment, nested statement lists, precise locations, block comments, directive-specific messages, and reuse of the shared AST member and unwrap helpers.
- **Context, profiles, and bounded rule contracts — plan 012.** This plan covers empty or contradictory surfaces, filename-token boundaries, unknown-surface silence, mixed UI Actions, full-script Business Rule wrappers, flat-profile globs, per-loop `prefer-glideaggregate` proof, temporal bypass provenance, settings mutation, actionable messages, affected rule metadata, one runtime settings field authority, one confidence order, the weak-constructor rule factory, and removal of the unused object-option parser.
- **Public API, catalog, examples, docs, and repository quality — plan 013.** This plan narrows root exports, completes catalog descriptors, records preset migration changes, fixes copied examples and oxfmt fixtures, corrects compatibility and release text, adds self-lint and format gates, and renames the cryptic catalog evidence constructor.
- **Tests, evidence, compatibility, and performance — plan 014.** This plan replaces tautological matrices, adds exact real Oxlint and ESLint diagnostics, tests invalid options in Oxlint, derives Fluent boundary evidence from complete tarballs, binds ledger rows to test names and heads, generates the workflow matrix, bounds peer support, validates benchmark JSON, compares merge-base baselines, uploads current measurements, and prevents stale `dist` from satisfying a compatibility pack.
- **Release governance and provenance — plan 015.** This plan fixes tag-only environment policy, independent approval, tag-creation restrictions, CodeQL governance wording, npm checker duplication, typed retries, exact tag and commit checks, Sigstore identity verification, and a real protected prerelease proof.
- **Stable 2.0 release — plan 016.** This plan validates the merged stack, rechecks governance, publishes or reuses a release candidate, creates the immutable stable tag, verifies registry bytes and public imports, creates the GitHub release, records live evidence, and only then closes the release blockers.

No confirmed finding from the two PR #51 reviews is intentionally left without an implementation or operator plan.

## Reviewed claims that changed before planning

- The supplied review said `@servicenow/sdk@4.11.0` and `@servicenow/sdk-core@4.11.0` were unpublished. Primary npm metadata on 2026-08-20 showed both versions published on 2026-08-19 and tagged `latest`.
- Plan 010 therefore does not downgrade the default solely on that stale claim. It still adds `4.10.1` support and makes current-version selection depend on reviewed registry integrity and declarations.

## Historical backlog from the 2026-08-19 audit

The section below is retained for audit history. Plans 006–016 supersede every overlapping item. Remaining product-direction ideas are optional and are not PR #51 release requirements.

### Items retained from the original backlog

Correctness / rules:
- **Function-scoped GlideRecord state**: `validate-gliderecord-calls` and `prefer-glideaggregate` track bindings in one per-file map keyed by variable name, so state leaks between functions (a `gr` in one function satisfies `.query()` for another's; a count-only loop taints a later work loop). M effort, MED risk — needs a scope stack.
- **`no-promise` `.then/.catch/.finally` heuristic**: flags any object's method with those names (error severity). Tightening loses some detection; a maintainer call on heuristic strictness.
- **`no-complex-fluent-logic` gaps**: statement limit applies to arrow bodies only; `function(){}` and `class {}` expressions bypass it; the `>2` threshold is hardcoded and undocumented.
- **`fluent-directives`**: dangling `@fluent-ignore` detected only at EOF; comment `loc` branch reports column 0 under real oxlint (harness never covers that branch).

Testing depth:
- Exact-message/location assertions (harness falls back silently on unknown messageIds and empty `{{data}}` holes); exact-count default in `assertInvalid`.
- Valid-code cases for the six invalid-only engine rules (`no-at-method`, `no-packages-calls`, `no-weak-references`, `no-async-iterators`, `no-typed-arrays`, `no-proxy`) — plan 004 adds some for `no-packages-calls` only.
- Settings coverage: `allowedSysIds` (settings path), `allowedTables`, `scopePrefix` are untested; `settings.ts` and `ast.ts` (210 lines, 18 helpers) have no unit tests.
- Multi-file / multi-rule lint paths: `createOnce` is re-invoked per `applyRules` call, so cross-file state contamination is structurally untestable today; split the harness into `createLinter()` + `lintFile()`.
- Dist smoke test: import every `exports` subpath from the built `dist/` after `npm run build`; assert `npm pack` contents.

Docs / single source of truth:
- Generate the README rule tables, the recommended-rules JSON block, and `examples/.oxlintrc.json` from `ruleCatalog` + `recommendedRules` behind the existing `docs:check` gate; likewise generate `oxfmt.recommended.json` from `src/oxfmt/recommended.ts`.
- Document the 10 undocumented rule options across 5 rules (`allowedSysIds`, `ignoreHashNames`, `allowedTables`, `allowBuiltins`, `maxLines`, `maxChars`, `idStyle`, `fileStyle`, `preferNowId`) — add an Options section to the doc generator.
- `docs:check` holes: untracked new docs pass (`git diff` misses them); orphaned docs of removed rules are never deleted.
- CONTRIBUTING step 5 says to hand-write `docs/rules/*.md`, which are generated — actively misleading; also add an `AGENTS.md` (verification loop, generated-file map, `PACKAGE_VERSION` coupling, rule-count invariant).
- Ship `docs/` in the npm tarball (README's 24 rule links 404 inside `node_modules`) and version `DOCS_BASE_URL` (`blob/main` → `blob/v<version>`).
- Migration table: no provenance/links for the upstream plugins' rule ids; omits `no-hardcoded-table-names` and `no-async-iterators`.
- `applyRules` is exported public API with no docs, hardcoded `error` severity, and ignored fixes — document it or un-export it.
- Widened `scriptType` docs land with plan 002.

Tech debt:
- Shared factory for the five structurally identical engine-ban rules; delete the dead-code `active`-flag idiom in four rules (the `before() === false` skip already prevents visitor calls).
- Derive `FLUENT_ENTITIES_REQUIRING_ID` from `FLUENT_CORE_APIS` minus a two-entry exclusion set (currently hand-duplicated 28-entry lists).
- Dead exports: `isIdentifier`, `calleeName`, `isExpressionStatementCall`, `commentText`, `CommentLike` (ast.ts), `isEstreeNode` (apply-rules.ts), `getRuleCatalogEntry` (catalog.ts — or export it deliberately), `FLUENT_CORE_API_SET` (constants.ts).
- Split `src/catalog.ts` (556 lines) into per-rule example fixtures + an assembler; derive `preset`/`severity`/`fixable`/`hasSuggestions` from rule meta and configs instead of restating them.
- `no-complex-fluent-logic` lists its ten banned node types twice; `src/utils/filenames.ts` is a policy module under a utility name (split into `script-kind.ts`).
- `fallbackComments` and offset→line/col helpers duplicated across `apply-rules.ts` and `fluent-directives.ts` (plan 002 consolidates `fallbackComments` if taken).

Harness robustness (test-only code, exported as `applyRules`):
- Unbounded recursion in `walk` (deep ASTs overflow the stack); shared `ancestors` array corrupts on a throwing visitor (no try/finally); `lineCol` re-slices the file per diagnostic (quadratic); message interpolation has no length cap or control-character stripping; docs generator lacks path/fence escaping guards.

DX:
- The linter does not lint itself: no root oxlint/oxfmt config, no `lint`/`format` scripts (needs plan 001's devDeps); `examples/` neither typechecked nor executed (001 partially fixes by using them as fixtures); no `.editorconfig`, no pre-commit hook, no `test:watch`/coverage script; no issue templates.

Direction options (maintainer strategy calls — evidence in the audit):
- Rule-explorer/playground site over the exported `ruleCatalog` + `applyRules` (the code already references a "documentation playground"; requires fixing `applyRules` severity/fix fidelity first).
- Script Include / UI Action rule families (the classifier detects kinds no rule consumes; `scriptType` widening in plan 002 is the enabler).
- `init`/`migrate` CLI (`bin` entry) absorbing the README's five manual setup steps and the 15-row migration table.
- sys_id allowlist "collect mode" for brownfield adoption of `no-hardcoded-sysid`.
- Broader autofix coverage once plan 003's fix harness lands (`no-unsupported-syntax` `?.`/`??` rewrites as suggestions, `require-fluent-id` `$id` insertion).

## Findings considered and rejected

- **oxfmt formatting plugin**: oxfmt has no plugin API; the config preset is the documented ceiling (README states this). Not a gap.
- **sys_id values echoed into diagnostics**: the value is already a literal in the scanned source; showing it is what makes the rule actionable.
- **`interpolate` prototype-chain key lookup**: message templates are first-party; no untrusted input reaches the key position. Free hardening if the file is touched anyway.
- **Merging `applyRules`' per-rule AST walks / `Object.keys` allocation**: test-only, unmeasurable win at current suite size.
- **`dist/` committed to git** (recon suspicion): false — `dist/` is gitignored and untracked; the real gap is the missing `prepack` (plan 005).
- **CHANGELOG double-dated releases**: verified correct — 1.0.0 and 1.1.0 were released the same day (git timestamps 05:18 and 06:14).
- **`pull_request_target` misuse**: not present — CI correctly uses `pull_request`; plan 005 adds the remaining defense in depth.
