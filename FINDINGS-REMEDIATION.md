# FINDINGS.md remediation ledger

Disposition of every active record in `FINDINGS.md` (super-review of
2026-08-29). The review ran against the `pr51-remediation/layer6` worktree,
an older sibling of the plan-007-to-015 work that had already merged to
`main`. Each record was therefore re-verified against `main` before porting:
some defects had been fixed on `main` independently, others still
reproduced and are fixed by this branch.

## Verification state on this branch

- Hermetic `npm test` passes; no network access points remain in the
  default suite (the consumer install moved to `npm run test:consumer`).
- The release provenance verifier validates the real published 2.0.0
  attestation end to end with the enriched OIDC subject
  (`main` fixed this independently; re-proven during the port).
- The governance audit returns `ok` against live GitHub state, including
  the new required `consumer` check on ruleset 21081867.
- `lint:check`, `format:check`, `typecheck`, `typecheck:fixtures`,
  `docs:check`, `manifest:check`, `workflow:check`, `compat:check` pass.

## Dispositions

| ID | Disposition | Note |
| --- | --- | --- |
| OPS-001 | Ported | Config files were already tracked on main; `scripts/check-script-paths.mjs` guard added so a script-referenced untracked path fails CI. |
| OPS-002 | Ported (test) | The one wrong check name was already fixed on main; the producible-check-name subset test is new. Live ruleset verified against the audit. |
| OPS-003 | Resolved | The reviewed worktree is committed on `pr51-remediation/layer6`; its still-needed content is ported here. |
| OPS-004 | Ported | `npm test` is hermetic; the packed-consumer test runs as `test:consumer`, its own CI job, and a release validate step with `--ignore-scripts`. Main had already moved the examples config out of the repository. |
| OPS-005 | Fixed on main | Real principals and the reviewer normalization were already on main; the self-validation test was too. |
| OPS-006 | Fixed on main | Main removed the `PACKAGE_VERSION` root-export probe and verifies DER-decoded Fulcio extensions with the ID-enriched 1.24 subject; re-proven against the real npm 2.0.0 attestation. The packed-consumer contract test for the probe is ported. |
| OPS-007 | Ported | Nightly CI schedule plus the scheduled `manifest-drift` job. |
| TST-001 | Fixed on main | The fixture clock already tracks wall time. |
| TST-002 | Ported | The 1.1.0 preset source is vendored; the digest is asserted from committed bytes, so a shallow CI checkout passes. |
| COR-001 | Ported | Directory conventions match the project-relative path bounded by `context.cwd`; absolute decoy segments no longer classify. Adapted to main's richer surface patterns (acl, ui-action, scheduled, fix-script). |
| COR-002 | Ported | 32-hex suppression for every digest-like owner name, on top of main's structural `valueOwnerName`. |
| COR-003 | Fixed on main | Constant-condition pruning probed correct on main; the four symptom classes verified. |
| COR-004 | Fixed on main | Synchronous IIFEs already inherit loop context on main. |
| COR-005 | Fixed on main | Receiver state is keyed by binding/object identity; the dead display-name correlator never reached main. |
| COR-006 | Ported | Fluent alias resolution follows execution order; nested-function writes and function-scoped uses are conservatively uncertain. |
| API-001 | Fixed on main | `analyzeProvenance` already threads its AST argument (`getFileAnalysisForAst`). |
| REL-001 | Ported | Prerelease parsing splits at the first hyphen only; hyphenated identifiers order correctly. |
| REL-002 | Ported | 120 s per-operation timeouts (npm children, import probe, attestation fetch abort signal) and `timeout-minutes` on every job in all five workflows. |
| PER-001 | Ported | Interface annotation instead of `as const` on the generated snapshot; 200 KB per-declaration budget in the artifact check. Main's richer snapshot schema (typos, lifecycle) preserved. |
| MNT-001 | Ported | The unreachable autofix harness is removed; catalog metadata stays as host-facing truth. |
| MNT-002 | Ported | `src/version.ts` is generated by prebuild; no load-time filesystem read behind `sideEffects: false`. |
| MNT-003 | Fixed on main | Constructor lookups already guard with `hasOwnProperty`. |
| DOC-001 | Ported | The ledger's generated outputs are excluded from its worktree digest, so the digest is reproducible. |
| DOC-002 | Ported (test) | The Xanadu URLs were already fixed on main; the unsupported-release assertion over evidence URLs is new. |
| IMP-001 | Ported (delta) | Main's checker already parses workflows structurally; composite `action.yml` files are now scanned too. |
| IMP-002 | Ported | Bounded npm range `>=11.5.1 <12`; the publish job tracks the pipeline Node version. |
| FEAT-001 | Decision recorded | Thin presets stay through 2.x; 3.0 review trigger in `docs/decisions.md`. |
| FEAT-002 | Decision recorded | The 1.x settings layer retires in 3.0 per `docs/decisions.md`. |
| REM-001 | Decision recorded | `validate-gliderecord-calls` removal in 3.0 announced in the changelog, rule page, and README. |
| POS-001..004 | Preserved | The protected patterns are untouched; correctness fixes only narrow wrong evidence. |
